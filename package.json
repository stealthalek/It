{
  "name": "it-ticketing-platform",
  "version": "1.0.0",
  "description": "Piattaforma di ticketing full-stack, responsive e utilizzabile da qualsiasi dispositivo",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build:frontend": "rm -rf dist && mkdir -p dist && cp -r public/. dist/ && npx --yes terser@5 public/js/app.js --compress --mangle --output dist/js/app.js && npx --yes clean-css-cli@5 -o dist/css/style.css public/css/style.css && node scripts/stamp-service-worker.js",
    "backup:db": "node server/scripts/backup-db.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "google-auth-library": "^9.14.1",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "nodemailer": "^9.0.6",
    "socket.io": "^4.8.3"
  }
}
